name: Meson Build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
     - name: Meson Build
       uses: BSFishy/meson-build@v1.0.3
          
  
